[project]
name = "safe-llm-endpoint"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.116.1",
]

[dependency-groups]
dev = [
    "ruff>=0.12.4",
]
